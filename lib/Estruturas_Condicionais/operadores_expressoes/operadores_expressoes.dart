void main(List<String> args) {
  // Operadores

  // > Maior que
  // < Menor que
  // >= Maior ou igual a
  //  <= Menor ou igual a
  // == Igual a
  // != Diferente de
  //  Expressão Relaciona l ==>   ( 3 < 4)
  // ==================================

  var idade = 40;
  print(idade > 18); // true

  // && E lógico
  // || Ou lógico
  // ! Negação lógica
  // % Resto da divisão
  // + Adição
  // - Subtração
  // * Multiplicação
  // / Divisão
  // ~/ Divisão inteira
  // ++ Incremento
  // -- Decremento
  // = Atribuição
  // ?? Atribuição condicional
  // ??= Atribuição condicional composta
}
